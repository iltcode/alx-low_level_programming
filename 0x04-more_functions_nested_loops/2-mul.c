#include "main.h"
/**
 * mul - Multiplies two integers
 * @x: The first number to multiply
 * @y: The second number to multiply with first number
 * Return: Ther result of multiplication between x param and y param
 */
int mul(int x, int y)
{
	int a;

	a = x * y;
	return (a);
}
